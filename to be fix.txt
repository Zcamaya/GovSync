note

in the note component i can click the notes header and it will go to the notes window showing a grid stack of notes card there is a 3 circles int he top right of it green yellow red i cant select only one of them if its click the red that notes is cannot be deleted and in high priority also add a due date notification in the note editor add a date and it will notify the users once that date is same in the current date i can leave it blank also


in the dashboard at notification board there that is connected with this notes also the total employee for the current month uploaded for sss and philhealth 



























# SYSTEM REFACTORING & FEATURE ENHANCEMENT SPECIFICATIONS

## 1. Global / Auth & Account Management
* **Account Creation Form:** * Add inputs: `SSS Number`, `PhilHealth Number`, `HDMF Number`.
* **Database/State Linking:** * Bind the `SSS Number` from the user account directly to the SSS module pages.

## 2. Dashboard & Navigation
* **Top Header:** Add a welcome banner rendering: `"Welcome, [Account Name]"`.
* **Superadmin Files:** Update the modal/dialog box text and layout inside the `GovSync` component.
* **Auth Pages (Login):** Clear/reset all validation and error messages whenever the login page mounts/opens.

## 3. PhilHealth Module
* **Data Persistence:** Ensure the `Total Metric Record` persists globally across account switches.
* **History Card Redesign:**
    * Add `Date Created` timestamp.
    * Relocate the `Delete` action to the top-left corner, represented as an `X` icon button.
* **UI Alignment:** Sync the progression bar UI component to match the styling/logic of the SSS progression bar.
* **Record Viewer Optimization:**
    * Remove `Missing Names` and `Newly Hired` lists from the main viewer (this data is already handled in the Process section via card clicks).
    * **Card Click Behavior:** Clicking a card must directly render the data table.
    * **Table Enhancements:** * Add an auto-incrementing index column labeled `"No."` (1 to N).
        * Implement draggable/movable rows for the table.
    * **UX Fix:** Remove the confirmation dialog box when exiting the Record Viewer.

## 4. SSS Module & Automation Logic
* **Table UI:** Make the imported SSS data table fully inline-editable.
* **Mass Automation (Excel):** * Implement bulk processing to automate SSS worksheet generation across multiple selected Earnings Excel files.
* **Data Parsing & Delta Sync:**
    * Add JSON export/import capability (matching PhilHealth's implementation).
    * **Hiring Logic Reform:** Parse the past month's uploaded data against current data. Identify `Newly Hired` and `Missing Employees` by cross-referencing `Date Hired` against historical past-month records.

## 5. Right Panel: Notes Component Refactor
* **UI Overhaul:** Replace the static note area with a dynamic checklist/items list.
* **List Actions:** Add a `+` (Add Item) button. Each listed item must feature an `X` (Delete) button on its left.
* **Note Modal / Window:** Clicking an item opens a dedicated editor window.
* **Editor Features:**
    * Separate fields for `Title` and `Content`.
    * The `Title` field value must dynamically mirror back to the main item list view.
    * **Rich Text Support:** Implement formatting for bold, italic, underline, and bulleted/unordered lists on new lines.

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

# Role & Context
You are an expert full-stack developer working on the "GovSync" enterprise platform. Your task is to implement a comprehensive set of system refactoring and feature enhancement specifications across multiple modules. Adhere strictly to the domain logic, UI parameters, and fallback rules provided below.

---

## 1. Global / Auth & Account Management
* **Account Creation Form:** Add three new input fields: `SSS Number`, `PhilHealth Number`, and `HDMF Number`.
* **Database/State Linking:** Bind the `SSS Number` globally from the user's active account state directly into the SSS module context/pages.

---

## 2. Dashboard & Navigation
* **Top Header Component:** Implement a new top-level welcome banner that dynamically renders string: `"Welcome, [Account Name]"`.
* **Superadmin Files:** Update the text copy layout and styling of the modal/dialog box elements located internally within the `GovSync` component view.
* **Auth Pages (Login):** On the login page lifecycle hook (`componentDidMount` or `useEffect` on empty dependency array), ensure all existing validation and error messages are cleared/reset immediately upon page mounting.

---

## 3. PhilHealth Module
* **Data Persistence:** Ensure the `Total Metric Record` variable is managed globally so it persists state across account switching cycles.
* **History Card Redesign:**
  * Embed a `Date Created` timestamp badge on each card.
  * Relocate the `Delete` action button to the absolute top-left corner of the card component, replacing text links with an standard `X` icon button.
* **UI Alignment:** Refactor the progression bar UI component to visually and logically match the implementation used in the SSS progression bar.
* **Record Viewer Optimization:**
  * Strip out the `Missing Names` and `Newly Hired` lists from the main viewer interface (this data parsing is already isolated in the Process workflow via card click actions).
  * **Card Click Behavior:** Clicking an individual history card must bypass intermediate menus and directly render the corresponding data table.
  * **Table Enhancements:** 
    * Prepend an auto-incrementing index column labeled `"No."` (spanning rows 1 to N).
    * Integrate a drag-and-drop row sorting utility to allow fully draggable/movable rows.
  * **UX Fix:** Remove the exit confirmation modal dialog completely when a user closes or routes away from the Record Viewer.

---

## 4. SSS Module & Automation Logic
* **Table UI:** Convert the imported SSS data table grid into a fully inline-editable system.
* **Mass Automation (Excel):** Implement bulk background processing queues capable of automating sequential SSS worksheet generation across multiple user-selected Earnings Excel spreadsheets.
* **Data Parsing & Delta Sync (Hiring Logic Reform):**
  * Introduce JSON export and import capabilities mirroring the exact implementation strategy used within the PhilHealth module.
  * **Delta Parsing Rule:** Implement an historical cross-referencing comparison engine between the past month's uploaded dataset and the current month's active payload.
    * **Condition A (Match Found):** Identify `Missing Employees` if a record exists in the historical past-month file but is absent in the current payload.
    * **Condition B (Newly Hired Rule):** Flag an employee as `Newly Hired` if and only if that employee does NOT exist anywhere within the past month's historical data snapshot.
    * **Condition C (Fallback Rule):** If there is no historical past-month data file available to match or cross-reference against, **do not flag any employees as newly hired**. Default their status safely to normal/standard state.

---

## 5. Right Panel: Notes Component Refactor
* **UI Overhaul:** Replace the current static layout notes placeholder with a highly interactive, dynamic checklist/item list component.
* **List Action Hooks:** Add a persistent `+` (Add Item) button block at the list tier. Every list row instance generated must feature an immediate execution `X` (Delete) icon button bound to its absolute left boundary.
* **Note Modal Editor:** Tapping any list entry row must trigger a dedicated modal popup/window display instance.
* **Editor Features:**
  * Segregate input controls into distinct, dedicated entry form fields for `Title` and `Content`.
  * Establish a reactive bi-directional sync (or upward state emission) so changes typed inside the modal's `Title` field dynamically mirror back onto the main parent list view row item in real-time.
  * **Rich Text Support:** Support formatting shortcuts and engines for bold, italic, underline decorations, along with dynamic bulleted/unordered list structures generated cleanly on new line insertions.

---

# Execution Requirements
1. Analyze the project layout directory map.
2. Identify all relevant state management contexts, API route handlers, and UI file components.
3. Apply code modifications adhering to strict type parameters, idiomatic code syntax, and clean architecture abstractions. Do not omit necessary verification rules.


# SYSTEM REFACTORING & FEATURE ENHANCEMENT SPECIFICATIONS